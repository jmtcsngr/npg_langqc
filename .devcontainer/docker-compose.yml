# npg_langqc devcontainer stack.
#
# Step 1: `workspace` service only — this is where VS Code / Codespaces attaches
# and where the repository is bind-mounted. The MLWH + QC databases, the backend
# API, the frontend and the Apache proxy are added as sibling services in later
# steps of the devcontainer plan (see issue #1).
#
# Generated by:    Claude Opus 4.8 (1M context)
# Generation date: 2026-07-31

services:
  workspace:
    image: mcr.microsoft.com/devcontainers/python:3.10
    # Mount the parent of the repo at /workspaces so the repo lands at
    # /workspaces/<repo-name>, matching devcontainer.json's workspaceFolder.
    volumes:
      - ../..:/workspaces:cached
    # Keep the container alive; the editor drives everything else.
    command: sleep infinity
